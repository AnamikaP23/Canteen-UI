Create database bfc;

Use bfc;



create table students(REGNO varchar(30) primary key, NAME varchar(100) not null, PASSWORD varchar(100) not null);



create table menu(FOODID int primary key, FNAME varchar(50), PRICE float);



create table orders(TOKEN_NO int primary key, REGNO varchar(30) NOT NULL, FOODID int NOT NULL, QTY int NOT NULL, AMT_PAID float NOT NULL, ORDER_TIME timestamp default current_timestamp,FOREIGN KEY(REGNO) REFERENCES students(REGNO), FOREIGN KEY(FOODID) REFERENCES menu(FOODID));

ALTER TABLE orders ADD COLUMN DELIVERED VARCHAR(3) DEFAULT 'NO';

insert into orders(TOKEN_NO, REGNO, FOODID, QTY, AMT_PAID) values(0, 'xxxx', 1,0,0.00);